---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			.calcul {
				width: calc(50% - 1rem);
			}

			ul li a:hover h4,
			ul li a:hover .date {
				color: rgb(var(--accent));
			}
			ul a:hover img {
				box-shadow: var(--box-shadow);
			}
		</style>
	</head>
	<body class=" bg-white-light">
		<Header />

		<p></p>

		<main class=" pb-20">
			<section class="container">
				<!-- Tag -->
				<div class="container">
					<ul class=" flex flex-row flex-wrap gap-1 justify-start pb-2 pr-40 md:pr-20">
						<li class=" px-4 py-1 max-w-max rounded-lg bg-[#D9A324] hover:bg-[#B5881E] text-white text-sm">#Réseaux Sociaux</li>
						<li class=" px-4 py-1 max-w-max rounded-lg bg-[#D9A324] hover:bg-[#B5881E] text-white text-sm">#Graphisme</li>
						<li class=" px-4 py-1 max-w-max rounded-lg bg-[#D9A324] hover:bg-[#B5881E] text-white text-sm">#Photographie</li>
						<li class=" px-4 py-1 max-w-max rounded-lg bg-[#D9A324] hover:bg-[#B5881E] text-white text-sm">#Webdesign</li>
						<li class=" px-4 py-1 max-w-max rounded-lg bg-[#D9A324] hover:bg-[#B5881E] text-white text-sm">#Montage</li>
					</ul>
				</div>

				<!-- Card -->
				<ul class=" flex flex-wrap gap-8 list-none m-0 p-0 md:gap-4">
					{
						posts.map((post) => (
							<li class="calcul first:basis-full first:mb-4 first:text-center md:w-full md:text-center md:first:mb-0">
								<a class=" block" href={`/blog/${post.slug}/`}>

									<img class=" w-full h-72 mb-2 rounded-xl" src={post.data.heroImage} alt="" />
									<h3 class=" m-0 text-black md:first:text-2xl">{post.data.title}</h3>
									<p class=" m-0 text-black">
										<FormattedDate date={post.data.pubDate} />
									</p>

								</a>
							</li>
						))
					}
				</ul>
			</section>
		</main>

		<Footer />
	</body>
</html>
